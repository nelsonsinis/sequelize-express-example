const booksRepository = require('./books');
const orderRepository = require('./order');
const personRepository = require('./person');

module.exports = {
  booksRepository,
  orderRepository,
  personRepository,
};
